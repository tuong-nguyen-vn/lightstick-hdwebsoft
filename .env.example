# Server Configuration
PORT=3001
NODE_ENV=development

# CORS - comma-separated origins or * for all
CORS_ORIGIN=*

# Admin Authentication Secret
ADMIN_SECRET=lightstick-admin-secret

# Fly.io specific (set via `fly secrets set`)
# FLY_APP_NAME=lightstick-hdwebsoft
